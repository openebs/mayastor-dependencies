[package]
name = "version-info"
version = "0.1.0"
edition = "2018"

[dependencies]
git-version-macro = { path = "../git-version-macro" }
regex = { workspace = true }

[features]
default = ["deps"]
deps = []
deps-logs-head = ["deps"]
deps-index = ["deps"]
