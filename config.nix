/*
  This file contains default values for parameters used in nix derivations.
  Set a value here if you want to share a nix value across all of the repos
  which use this repo as a git submodule.
*/
{
  product_prefix = "mayastor";
}
